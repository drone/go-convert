version: 1

stages:
- desc: converted from travis.yml
  name: pipeline
  type: ci
  spec:
    steps:
    - name: install
      type: script
      spec:
        run: rebar get-deps
    - name: script
      type: script
      spec:
        run: rebar compile && rebar skip_deps=true eunit
