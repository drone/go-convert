# https://cloud.google.com/build/docs/samples/cloudbuild-python-dependencies-yaml

steps:
  # Install dependencies
  - name: python
    entrypoint: pip
    args: ["install", "-r", "requirements.txt", "--user"]
